/* Phal AutoGenerated .ino file 
*  Created 14-05-2018
*/

#include "Lightbulb.h" 
#include "TemperatureSensor.h" 
bool satan = true;
bool god = false;
bool alive = true;
bool dead = false;
void setup(){ 
pinMode(1, OUTPUT);
pinMode(2, OUTPUT);
pinMode(5, OUTPUT);
pinMode(2, INPUT);
dead=true;
Delay((32)*1000);
} 

void loop(){ 
} 

